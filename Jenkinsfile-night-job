pipeline {
    agent any

    stages {

        stage ('Build Docker image'){
            steps{
                sh '''
                echo "Building Docker image..."
                docker build -t my-playwright .
                '''
            }
        }
    }
}